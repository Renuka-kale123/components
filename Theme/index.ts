import { colors, Icon } from './Colors';
import { fonts, sizes, SIZES } from './Fonts';
import { images } from './Images';
import { textStyles } from './TextStyles';
import { viewStyles } from './ViewStyles';

export const cropwiseStyle = {
  colors,
  fonts,
  images,
  viewStyles,
  textStyles,
  sizes,
  SIZES,
  Icon
};
